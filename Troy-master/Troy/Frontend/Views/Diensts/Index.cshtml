@model IEnumerable<Frontend.Models.Dienst>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dienst)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.zorg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resortid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dienst)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.zorg)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resortid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
